import type { Metadata } from "next"; import { PageShell } from "../components/SiteChrome"; import { bookUrl } from "../data"; import { getSiteContent } from "../cms";
export const metadata:Metadata={title:"Client Reviews | ReviTouch NYC",description:"Read what New York clients say about personalized massage therapy at ReviTouch."};
export default async function Reviews(){const {reviews}=await getSiteContent();return <PageShell><section className="page-hero"><p className="eyebrow">Client stories</p><h1>Care people come back to.</h1><p>Thoughtful treatment, skilled touch, and an experience shaped around each client.</p><div className="review-summary"><strong>4.9</strong><span>★★★★★</span><small>Trusted by New Yorkers</small></div></section><section className="review-wall">{reviews.map((review)=><blockquote key={`${review.author}-${review.quote}`}><span>“</span>{review.quote}<cite>— {review.author}</cite></blockquote>)}</section><section className="page-cta"><div><h2>Ready to feel the difference?</h2><p>Choose the session length that works for your day.</p></div><a className="button light-button" href={bookUrl}>Book your treatment</a></section></PageShell>}
