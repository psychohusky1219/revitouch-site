import type { Metadata } from "next"; import { PageShell } from "../components/SiteChrome"; import { bookUrl } from "../data"; import { getSiteContent } from "../cms";
export const metadata:Metadata={title:"Massage Pricing | ReviTouch NYC",description:"View ReviTouch massage appointment lengths and current pricing."};
export default async function Pricing(){const {appointments,general}=await getSiteContent();return <PageShell><section className="page-hero"><p className="eyebrow">Clear, flexible pricing</p><h1>Choose the time your body needs.</h1><p>From a focused reset to a full 90-minute session, ReviTouch makes it easy to fit personalized care into your schedule.</p></section><section className="full-price-grid">{appointments.map((appointment)=><a href={bookUrl} key={appointment.slug}><p>{appointment.name}</p><span>{appointment.duration}</span><strong>{appointment.price}</strong><small>Book appointment →</small></a>)}</section><section className="booking-note"><div><h2>Need another time?</h2><p>If you do not see your preferred appointment online, call or text <a href={`tel:${general.phoneLink}`}>{general.phoneDisplay}</a>. We may be able to fit you in.</p></div><div><h2>Cancellation policy</h2><p>Please cancel at least 12 hours before your appointment. Late cancellations may be charged in full.</p></div></section><section className="page-cta"><div><h2>Give someone time to feel better.</h2><p>ReviTouch gift cards are available in flexible amounts.</p></div><a className="button light-button" href="/gift-cards">Shop gift cards</a></section></PageShell>}
